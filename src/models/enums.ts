export enum RoleType {
  Admin = 'admin',
  User = 'user',
}

export enum ActionType {
  Manage = 'manage',
  Read = 'read',
  Create = 'create',
  Update = 'update',
  Delete = 'delete',
}

export enum Scope {
  All = 'all',
  Articles = 'articles',
  Comments = 'comments',
  Users = 'users',
}
